rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /tenant-documents/{uid}/{tenantId}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
